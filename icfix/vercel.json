{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "echo \"VITE_ADMIN_BACKEND_URL is set to: $VITE_ADMIN_BACKEND_URL\" && echo \"VITE_BASE_PATH is set to: $VITE_BASE_PATH\" && node node_modules/@medusajs/cli/cli.js build --admin-only && npm run copy-admin",
  "outputDirectory": "admin",
  "rewrites": [
    {
      "source": "/((?!app/assets/).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
