# ===========================================
# Medusa Storefront Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values
# DO NOT commit .env.local to version control
# ===========================================

# ===========================================
# Medusa Backend Configuration
# ===========================================
# Backend API URL (where your Medusa backend is running)
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000

# Publishable API Key (get this from Medusa Admin after seeding)
# Go to: Settings → Publishable API Keys
# Or get from database after running seed script
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=

# ===========================================
# Region Configuration
# ===========================================
# Default region/country code (Vietnam)
NEXT_PUBLIC_DEFAULT_REGION=vn

# ===========================================
# Revalidation Secret (for webhooks)
# ===========================================
# Must match the REVALIDATE_SECRET in backend .env
REVALIDATE_SECRET=

# ===========================================
# Optional: Custom Base URL
# ===========================================
# NEXT_PUBLIC_BASE_URL=http://localhost:8000

# ===========================================
# Optional: Stripe (if using Stripe payments)
# ===========================================
# NEXT_PUBLIC_STRIPE_KEY=pk_test_...

# ===========================================
# How to get NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY:
# ===========================================
# 
# Option 1: From Admin UI (after seeding)
# 1. Run: docker exec -it icfix-backend npm run seed
# 2. Login to Admin: http://localhost:9000/app
# 3. Go to: Settings → Publishable API Keys
# 4. Copy the key (starts with pk_)
#
# Option 2: From Database
# 1. Run: docker exec -it icfix-backend npm run seed
# 2. Get key: docker exec -it icfix-postgres psql -U icfix_user icfix_db -c "SELECT token FROM api_key WHERE type='publishable' ORDER BY created_at DESC LIMIT 1;"
# 3. Copy the token value
#
# ===========================================

